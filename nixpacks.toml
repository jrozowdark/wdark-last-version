[build]
# Forzar npm install en lugar de npm ci para evitar problemas con dependencias nativas
cmd = "npm install --legacy-peer-deps && npm run build"

[start]
# Comando para iniciar la aplicación en producción
cmd = "npm start"

# Puerto que Dockploy debe exponer
port = 3002
