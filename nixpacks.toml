[build]
# Usar Node.js para buildear
cmd = "npm install && npm run build"

[start]
# Comando para iniciar la aplicación en producción
cmd = "npm start"

# Puerto que Dockploy debe exponer
port = 3002
